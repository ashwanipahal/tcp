import React from 'react';
import PointsClaimContainer from '@tcp/core/src/components/features/account/PointsClaim';

const Login = props => {
  return (
    <React.Fragment>
      <PointsClaimContainer {...props} />
    </React.Fragment>
  );
};

export default Login;
