import { css } from 'styled-components';

export default css`
  .store-name.store-name--details {
    text-transform: capitalize;
    font-size: 16px;
  }

  &__backlink {
    margin-left: 13px;
  }
`;
