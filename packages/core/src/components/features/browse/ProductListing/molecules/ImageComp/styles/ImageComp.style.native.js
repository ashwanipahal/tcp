import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  /* eslint-disable */
  imageStyle: {
    width: 164,
    height: 205,
    resizeMode: 'contain',
  },
  /* eslint-enable */
});

export default styles;
