import styled, { css } from 'styled-components';

const PageContainer = styled.View`
  margin-top: ${props => props.theme.spacing.ELEM_SPACING.XXS};
  flex: 1;
`;
const styles = css``;

export { styles, PageContainer };
