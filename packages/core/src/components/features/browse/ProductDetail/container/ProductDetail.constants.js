import { PRODUCT_DETAIL_ACTION_PATTERN } from '../../../../../constants/reducer.constants';

const SET_PRODUCT_DETAILS = `${PRODUCT_DETAIL_ACTION_PATTERN}SET_PLP_PRODUCTS`;
const FETCH_PRODUCT_DETAILS = `${PRODUCT_DETAIL_ACTION_PATTERN}GET_PLP_PRODUCTS`;
const SET_ADD_TO_FAVORITE = `${PRODUCT_DETAIL_ACTION_PATTERN}SET_ADD_TO_FAVORITE`;

const PRODUCTDETAIL_CONSTANTS = {
  SET_PRODUCT_DETAILS,
  FETCH_PRODUCT_DETAILS,
  SET_ADD_TO_FAVORITE,
};

export default PRODUCTDETAIL_CONSTANTS;
