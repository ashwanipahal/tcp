import { BUNDLEPRODUCT_ACTION_PATTERN } from '../../../../../constants/reducer.constants';

const SET_BUNDLE_DETAILS = `${BUNDLEPRODUCT_ACTION_PATTERN}SET_BUNDLE_DETAILS`;
const FETCH_BUNDLE_DETAILS = `${BUNDLEPRODUCT_ACTION_PATTERN}FETCH_BUNDLE_DETAILS`;
const SET_BUNDLE_PRODUCTS_DETAILS = `${BUNDLEPRODUCT_ACTION_PATTERN}SET_BUNDLE_PRODUCTS_DETAILS`;
const CLEAR_BUNDLE_DETAILS = `${BUNDLEPRODUCT_ACTION_PATTERN}CLEAR_BUNDLE_DETAILS`;

const BUNDLEPRODUCT_CONSTANTS = {
  SET_BUNDLE_DETAILS,
  FETCH_BUNDLE_DETAILS,
  SET_BUNDLE_PRODUCTS_DETAILS,
  CLEAR_BUNDLE_DETAILS,
};

export default BUNDLEPRODUCT_CONSTANTS;
