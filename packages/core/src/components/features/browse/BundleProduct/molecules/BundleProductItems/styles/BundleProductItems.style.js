import { css } from 'styled-components';

export default css`
  && .pdp-qty {
    padding-top: 2px;
  }
  .product-information {
    width: calc(100% - 30px);
    max-width: 100%;
  }
  && .button-wrapper {
    position: relative;
  }
`;
