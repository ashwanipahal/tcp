import { css } from 'styled-components';

export default css`
  && .pdp-qty {
    padding-top: 4px;
  }
  && .select__input {
    top: 0;
  }
  .product-information {
    width: calc(100% - 30px);
    max-width: 100%;
  }
  && .button-wrapper {
    position: relative;
    min-width: 100%;
  }
  .outfit-sku .size-error-message {
    max-width: 90%;
  }
`;
