import styled, { css } from 'styled-components/native';

const Style = css`
  width: 100%;
`;

const GovernmentId = styled.View`
  position: relative;
  top: -25px;
`;

export { Style, GovernmentId };
