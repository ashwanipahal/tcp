import { css } from 'styled-components';

const styles = css`
  .address-wrapper {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
  }
  .default-badge {
    height: 13px;
  }
  .margin-right {
    margin-right: auto;
  }
  .address-field {
    height: auto;
  }
`;

export default styles;
