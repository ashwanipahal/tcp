import { css } from 'styled-components';

const checkoutActionsStyle = css`
  margin: 0px;

  .checkout-button {
    font-weight: 800;
    margin-bottom: 10px;
    color: red;
  }
`;
export default checkoutActionsStyle;
