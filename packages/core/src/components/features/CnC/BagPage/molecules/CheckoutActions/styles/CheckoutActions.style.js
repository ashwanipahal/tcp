import { css } from 'styled-components';

const checkoutActionsStyle = css`
  .checkout-actions {
    margin: 0px;
  }

  .checkout-button {
    margin-bottom: 10px;
  }
`;
export default checkoutActionsStyle;
