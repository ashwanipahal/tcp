import React from 'react';
import ParentContainer from '../styles/AddressBook.style.native'; //eslint-disable-line

export default class AddressBook extends React.PureComponent<Props> {
  render() {
    return <ParentContainer />;
  }
}
