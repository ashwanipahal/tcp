export const DEFAULT_LAZY_LOAD_CONFIG = {
  thresholdX: 200,
  thresholdY: 0,
  throttleWait: 75,
  noLazyHorizontalScroll: false,
  callback: null,
  wrapperID: null,
};

export default DEFAULT_LAZY_LOAD_CONFIG;
