import { css } from 'styled-components';

const GlobalCss = css`
  font-family: Nunito;
  font-size: 13px;
  font-weight: normal;
  font-style: normal;
  font-stretch: normal;
  line-height: 1.15;
  letter-spacing: normal;
  color: #1a1a1a;
`;

export default GlobalCss;
