import { APPLY_NOW_MODAL_REDUCER_PATTERN } from '../../../../../constants/reducer.constants';

const APPLY_NOW_MODAL_CONSTANTS = {
  APPLY_NOW_MODAL_TOGGLE: `${APPLY_NOW_MODAL_REDUCER_PATTERN}TOGGLE`,
  APPLY_NOW_PLCC_MODAL_TOGGLE: `${APPLY_NOW_MODAL_REDUCER_PATTERN}PLCC_TOGGLE`,
};

export default APPLY_NOW_MODAL_CONSTANTS;
