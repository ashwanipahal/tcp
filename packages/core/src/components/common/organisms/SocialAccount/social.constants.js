import { SOCIAL_ACTION_PATTERN } from '../../../../constants/reducer.constants';

const SOCIAL_CONSTANTS = {
  GET_SOCIAL_LOAD_TTL: 60 * 1000,
  GET_SOCIAL_LOAD: `${SOCIAL_ACTION_PATTERN}GET_SOCIAL_LOAD`,
  SET_SOCIAL_LOAD: `${SOCIAL_ACTION_PATTERN}SET_SOCIAL_LOAD`,
  SAVE_SOCIAL_LOAD: `${SOCIAL_ACTION_PATTERN}SAVE_SOCIAL_LOAD`,
  POINT_MODAL_MOUNT_STATE: 'POINT_MODAL_MOUNT_STATE',
};

export default SOCIAL_CONSTANTS;
