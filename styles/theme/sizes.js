const UI_MAX_WIDTH = 1920;
const GRID_MAX_WIDTH = 1440;

const GUTTER_WIDTH = 30 * 2;

const HEADER_HEIGHT = 60;

export default {
  uiMaxWidth: `${UI_MAX_WIDTH + GUTTER_WIDTH}px`,
  gridMaxWidth: `${GRID_MAX_WIDTH + GUTTER_WIDTH}px`,
  headerHeight: `${HEADER_HEIGHT}px`,
};
